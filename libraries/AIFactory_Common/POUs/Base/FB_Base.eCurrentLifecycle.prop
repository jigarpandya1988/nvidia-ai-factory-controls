(* =============================================================================
    FB_Base.eCurrentLifecycle — I_Controllable Property Implementation
    Common Library | NVIDIA AI Factory Controls
   ============================================================================= *)

(* ═══ DECLARATION ═══ *)
PROPERTY eCurrentLifecycle : E_Lifecycle

(* ═══ IMPLEMENTATION (GET) ═══ *)
eCurrentLifecycle := eLifecycle;
