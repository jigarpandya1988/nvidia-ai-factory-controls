# NATS Bridge — Python Dependencies
nats-py==2.7.0              # NATS client (async)
asyncua==1.1.0              # OPC UA client (async)
pyyaml==6.0.1               # Configuration
structlog==24.1.0           # Structured logging
prometheus-client==0.20.0   # Metrics export
uvloop==0.19.0              # Fast event loop (Linux)
